{
  // Covers the TypeScript that belongs to the repository rather than to a
  // published package: the README asset generator. Each package has its own
  // tsconfig extending the same base, which also covers its tests.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["assets/**/*.ts"]
}
